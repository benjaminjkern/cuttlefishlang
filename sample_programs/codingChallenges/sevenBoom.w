sevenBoom = fn: [Num] numList ->
    numList.some fn: num -> String(num).includes("7")


some <-> any
every <-> all
includes <-> contains

        testString = `hello, {
    a + b +
        chiefKeef
}

`